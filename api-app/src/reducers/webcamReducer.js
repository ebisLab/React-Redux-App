const initialState = {
    webcams: [], 
    test: 'Its working mf*kas!!!! '
}

export const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}